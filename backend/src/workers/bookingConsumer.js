const start = () => {
  console.log('[BookingConsumer] Worker stub — not yet implemented');
};

const stop = async () => {
  console.log('[BookingConsumer] Worker stopped');
};

module.exports = { start, stop };
